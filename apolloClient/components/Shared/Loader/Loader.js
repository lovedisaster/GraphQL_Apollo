import React from 'react';
import {LoaderWrapper} from './LoderStyles'

const Loader = () => {
    return (
        <LoaderWrapper>
            <h5>Loading ...</h5>
        </LoaderWrapper>
        )
    }

export default Loader;